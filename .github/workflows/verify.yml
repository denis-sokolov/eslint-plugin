name: Verify

on: [push]

jobs:
  verify:
    runs-on: ubuntu-latest

    steps:
      - uses: actions/checkout@v2
      - uses: actions/setup-node@v4
        with:
          node-version: latest
      - name: npm install
        run: npm ci
      - name: verify
        run: npm run verify
